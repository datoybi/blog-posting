<!DOCTYPE html>
<html lang="en">
<head>
    <title>뷰어</title>
    <link rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css' />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src='/javascripts/view.js'></script>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css'/>
    
</head>
<body>    
    <div class="container">
        <h3>작성된 게시물</h3><br/><br/>
        <div class="mb-3 row">
          <label for="title" class="col-sm-2 col-form-label">제목</label>
          <div class="col-sm-10">
            <input type="text" class="form-control" name="title" value="<%=data.contents[0].title %>" disabled />
          </div>
        </div>
        <div class="mb-3 row">
          <label for="content" class="col-sm-2 col-form-label">내용</label>
          <div class="col-sm-10">
            <input type="text" class="form-control" name="content" value="<%=data.contents[0].contents %>" disabled />
          </div>
        </div>
        <div class="mb-3 row">
          <label for="content" class="col-sm-2 col-form-label">파일 첨부</label>
            <div class="col-sm-10">
              <% if(data.files) {%> 
                <% let i = 1 %>
                <% data.files.forEach(file => { %>
                <img class="viewer-image" id='img<%=i%>' src="http://192.168.1.55:3001/uploads/<%=file.name%>" onclick="imageClick(this)">
                <% i ++%>
                <% }) %>
              <% } %>
          </div>
        </div>      
        <div class="col-auto">
          <button type="button" id="deleteBtn" class="btn btn-primary mb-3">삭제</button>
          <button type="button" id="updateBtn" class="btn btn-primary mb-3">수정</button>
          <button type="button" id="backBtn" class="btn btn-primary mb-3">뒤로가기</button>
        </div>
        <hr>
      </div>
      <input type="hidden" id="postId" value ="<%= postId %>" />

      <!-- popup -->
      <div id="imgDetail" class="hide">
        <div class="modal-title">
          <i class="fa-solid fa-circle-xmark modal-close"></i>
          <p id="modalName"></p>
            <a href="" class="modal-download" download>
              <i class="fa-solid fa-download modal-download">
              </i>
            </a>
        </div>
        <div class="modal-contents">
          <div class="left-arrow">
            <i class="modal-arrow fa-solid fa-angle-left hide" onclick="plusSlides(-1)"></i>
          </div>
          <img id="modalSrc" class="modal-img"/>
          <div class="right-arrow">
            <i class="modal-arrow fa-solid fa-angle-right hide" onclick="plusSlides(1)"></i>
          </div>
        </div>
      </div>
      </div>
    </body>
</html>